# Data

No dataset files need to be manually copied here.

MNIST is automatically downloaded and cached by TensorFlow/Keras when `src/train.py` is executed.

The repository intentionally does not include the dataset itself.
